name: seleniumPatient

on:
  push:
    branches:
      - master
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Build backend Docker image
      run: docker build -t backend:latest ./backend

    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '16'

    - name: Install frontend dependencies
      working-directory: ./frontend
      run: npm install

    - name: Wait for backend to be healthy
      run: |
        until [ $(docker inspect --format='{{.State.Health.Status}}' $(docker ps -q --filter name=backend)) = "healthy" ]; do
          echo "Waiting for backend to be healthy..."
          sleep 2
        done

    - name: Run frontend tests
      working-directory: ./frontend
      run: npm test
      env:
        SELENIUM_REMOTE_URL: http://localhost:4444/wd/hub

    services:
      backend:
        image: backend:latest
        ports:
          - 9090:9090
        options: > 
          --health-cmd="curl --fail http://localhost:9090/actuator/health || exit 1"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5
        command: ["-p", "9090:9090"]  # Expose port 9090 during container runtime
      selenium:
        image: selenium/standalone-chrome:latest
        ports:
          - 4444:4444